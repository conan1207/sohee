import React from "react";
import "../css/NotFound.scss";

const NotFound = () => {
  return (
    <div className="not_found">
      <p>잘못된 경로로 접속하셨거나, 페이지를 찾을 수 없습니다.</p>
    </div>
  );
};

export default NotFound;
